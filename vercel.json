{
    "version": 2,
    "builds": [
      {
        "src": "nuxt.config.ts",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/"
      }
    ],
    "buildCommand": "NITRO_PRESET=vercel nuxt build --verbose"
  }